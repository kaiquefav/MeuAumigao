import React from 'react';
import * as S from './IconedTextInput.style';

import { Text } from 'react-native';

class IconedTextInput extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    const { iconFamily, iconName, placeholder, title } = this.props;
    return (



    );
  }
}

export default IconedTextInput;
